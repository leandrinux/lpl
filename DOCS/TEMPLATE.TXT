{
  unitname unit
  
  2025 LRT
}
unit
  unitname;

interface

  uses
    consts, utils, types, locale,
    uobject, uexc;

  const
    C_Something_CLASSNAME = 'TSomething';

  type
    PSomething = ^TSomething;
    TSomething = object (TObject)
    public
      constructor init;
      destructor done; virtual;
      function getClassName: string; virtual;
    private
    end;

implementation

  { TSomething public }

  constructor TSomething.init;
  begin
    inherited init;
  end;

  destructor TSomething.done;
  begin
    inherited done;
  end;

  function TSomething.getClassName: string;
  begin
    getClassName := C_Something_CLASSNAME;
  end;

  { TSomething private }

  { Other }

end.
